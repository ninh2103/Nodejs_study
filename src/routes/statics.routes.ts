import { Router } from 'express'
import {} from '~/controllers/medias.controller'
const staticRouter = Router()
// staticRouter.get('/video/:name', servingVideoController)
export default staticRouter
